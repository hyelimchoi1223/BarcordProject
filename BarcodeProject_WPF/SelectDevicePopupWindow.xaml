<Window x:Class="BarcodeProject_WPF.SelectDevicePopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BarcodeProject_WPF"
        mc:Ignorable="d"
        Title="SelectDevicePopupForm" Height="300" Width="535.714">
    <Grid>
        <DataGrid x:Name="DeviceGrid" Margin="10,10,9.6,10.4" MouseDoubleClick="DeviceGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="DeviceType" Binding="{Binding Path=DeviceType}"/>
                <DataGridTextColumn Header="DeviceName" Binding="{Binding Path=DeviceName}"/>
                <DataGridTextColumn Header="etc" Binding="{Binding Path=etc}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
